# Пошаговая инструкция: Создание базы данных Firestore

## Шаг 1: Откройте Firebase Console

1. Перейдите на https://console.firebase.google.com/
2. Войдите в свой аккаунт Google
3. Выберите проект **databaseonfirebase-bbb97** (или создайте новый)

## Шаг 2: Перейдите в Firestore Database

1. В левом меню найдите раздел **"Build"** (или **"Создание"**)
2. Нажмите на **"Firestore Database"**

## Шаг 3: Создайте базу данных

### Важно: Выберите правильный режим!

1. Нажмите кнопку **"Add database"** или **"Create database"**

2. **Выберите режим:**
   - ✅ **"Start in test mode"** - для тестирования (рекомендуется)
   - ❌ НЕ выбирайте "MongoDB compatibility" - это другой режим!

3. **Выберите регион:**
   - Рекомендуется: **europe-west** (Европа) или **us-central** (США)
   - Или выберите ближайший к вам регион

4. Нажмите **"Enable"** или **"Create"**

## Шаг 4: Проверьте создание базы данных

После создания вы должны увидеть:

- ✅ Вкладки: **Data**, **Indexes**, **Rules**, **Usage**
- ✅ Вкладка **Rules** должна быть доступна
- ✅ База данных с ID **"default"** или **(default)**

## Шаг 5: Настройте правила безопасности

### Вариант A: Через Firebase Console (веб-интерфейс)

1. Перейдите на вкладку **"Rules"**
2. Замените правила на:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2025, 12, 31);
    }
  }
}
```

3. Нажмите **"Publish"**

### Вариант B: Через Firebase CLI (из файла)

1. Убедитесь, что установлен Firebase CLI:
   ```bash
   npm install -g firebase-tools
   ```

2. Войдите в Firebase:
   ```bash
   firebase login
   ```

3. Инициализируйте проект (если ещё не сделали):
   ```bash
   firebase init firestore
   ```
   - Выберите существующий проект
   - Используйте файл `firestore.rules`

4. Загрузите правила:
   ```bash
   firebase deploy --only firestore:rules
   ```
   
   Или используйте скрипт:
   ```bash
   npm run deploy:rules
   ```

## Шаг 6: Проверьте подключение

Запустите тест:

```bash
node index.js
```

Если всё правильно, вы должны увидеть:
- ✅ Сообщения о создании документов
- ✅ Нет ошибок `PERMISSION_DENIED` или `NOT_FOUND`

## Возможные проблемы и решения

### Проблема: Не вижу вкладку "Rules"

**Решение:**
- Убедитесь, что база данных создана в режиме **"Start in test mode"** (не MongoDB)
- Если база в режиме MongoDB - удалите её и создайте заново

### Проблема: Ошибка "NOT_FOUND"

**Решение:**
- База данных не создана или создана в неправильном режиме
- Создайте базу данных заново в Native mode

### Проблема: Ошибка "PERMISSION_DENIED"

**Решение:**
- Правила безопасности не настроены
- Загрузите правила через Firebase Console или CLI

### Проблема: Требуется обновление тарифного плана

**Решение:**
- Вы пытаетесь создать именованную базу данных
- Создайте дефолтную базу (default) - она бесплатна
- Или обновите тариф (создание базы бесплатно, платятся только использование и дополнительные функции)

## Проверка в Firebase Console

После успешного создания:

1. Перейдите в **Firestore Database** → **Data**
2. Вы должны увидеть пустую базу данных
3. После запуска `node index.js` появятся коллекции и документы

## Готово! 🎉

Теперь ваша база данных готова к использованию в React приложении!

